# Lower bounds are the versions the code was written against; the upper bounds
# keep a future major release from breaking the build silently. Ranges rather
# than exact pins because the scientific stack ships wheels per Python version
# and this project supports 3.11 through 3.14.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
SQLAlchemy>=2.0.36,<2.1
psycopg[binary]>=3.2,<4.0
alembic>=1.14,<2.0
pandas>=2.2,<3.0
numpy>=2.1,<3.0
scipy>=1.14,<2.0
httpx>=0.28,<1.0
python-dotenv>=1.0,<2.0

# optional: Google Sheets export. Install only if you use it.
# gspread>=6.1,<7.0
# google-auth>=2.35,<3.0

# development / test
pytest>=8.3,<9.0
pytest-cov>=6.0,<8.0
